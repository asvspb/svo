Для создания такого приложения с помощью AI (например, через ChatGPT, Claude или Cursor) лучше всего использовать **модульный подход**. Не просите всё сразу — лучше строить систему по частям.

Вот серия промптов, которые проведут вас от исследования данных до готового Telegram-бота.

---

### Этап 1: Исследование и парсинг данных
**Цель:** Научить скрипт заходить на карту и скачивать актуальные гео-данные.

> **Промпт:**
> "Напиши скрипт на Python с использованием Playwright или Selenium, который имитирует посещение `https://deepstatemap.live/`. Скрипт должен перехватывать сетевые запросы (Network Tab) для поиска API-вызовов, возвращающих GeoJSON-данные с текущей линией фронта, оккупированными территориями и 'серыми зонами'. Если прямой API-доступ защищен Cloudflare, предложи способ парсинга координат слоев через выполнение JavaScript в контексте страницы. Результат должен сохраняться в файл `data_YYYY_MM_DD.json`."

---

### Этап 2: Обработка гео-данных и вычисление изменений
**Цель:** Сравнить два JSON-файла (вчерашний и сегодняшний) и найти разницу.

> **Промпт:**
> "У меня есть два GeoJSON файла с данными карты за разные даты. Используя библиотеки `geopandas` и `shapely`, напиши функцию, которая:
> 1. Сравнивает полигоны 'оккупировано' и 'серая зона' между двумя датами.
> 2. Вычисляет площадь изменившихся участков в квадратных километрах.
> 3. Определяет координаты центроидов (центров) этих изменений.
> 4. С помощью библиотеки `geopy` находит ближайшие к этим координатам населенные пункты (названия городов/сел).
> 5. На выходе выдает список объектов: `{направление: "Бахмутское", населенный пункт: "Ивановское", статус: "продвижение РФ", площадь: 0.5 км²}`."

---

### Этап 3: Формирование аналитического отчета
**Цель:** Превратить сухие цифры в читабельный текст.

> **Промпт:**
> "Напиши функцию на Python, которая принимает список изменений из предыдущего этапа. Функция должна:
> 1. Группировать мелкие изменения по регионам (направлениям).
> 2. Выделять ТОП-3 самых динамичных участка за сутки.
> 3. Формировать текст отчета для Telegram. Пример стиля: '🔴 За последние 24 часа зафиксировано продвижение противника в районе н.п. Очеретино (площадь: 1.2 км²). ⚪️ В районе Волчанска расширилась серая зона...'.
> 4. Если изменений нет, писать: 'На линии фронта без существенных изменений в конфигурации зон'."

---

### Этап 4: Создание Telegram-бота и автоматизация
**Цель:** Настроить доставку отчета и запуск по расписанию.

> **Промпт:**
> "Создай Telegram-бота на `aiogram` или `python-telegram-bot`, который:
> 1. По команде /start подписывает пользователя на ежедневную рассылку.
> 2. Использует библиотеку `schedule` или `APScheduler`, чтобы запускать процесс парсинга и сравнения данных раз в сутки в 21:00.
> 3. После обработки данных отправляет сформированный отчет всем подписчикам.
> 4. Добавь обработку ошибок (если сайт недоступен или формат данных изменился) с уведомлением администратора."

---

### Этап 5: Оптимизация и визуализация (Advanced)
**Цель:** Добавить в отчет мини-карты (скриншоты).

> **Промпт:**
> "Дополни скрипт функцией визуализации. Используя Playwright, сделай скриншоты тех участков карты (координат), где произошли наибольшие изменения. Скрипт должен:
> 1. Центрировать камеру на координатах изменения.
> 2. Установить оптимальный зум.
> 3. Сохранить скриншот и отправить его в Telegram вместе с текстовым описанием этого участка."

---

### Советы по реализации:
1.  **Библиотеки:** Стек Python (`playwright`, `geopandas`, `shapely`, `aiogram`) — самый надежный для этой задачи.
2.  **Cloudflare:** DeepState часто использует защиту от ботов. Возможно, вам придется запускать Playwright в режиме `headless=False` или использовать `stealth` плагины, чтобы сайт не распознал скрипт.
3.  **Хранение данных:** Для начала хватит папки с JSON-файлами, но для долгосрочной работы лучше использовать базу данных `PostgreSQL` с расширением `PostGIS` для работы с географией.
4.  **Хостинг:** Такое приложение удобно развернуть на простом VPS или через GitHub Actions (по расписанию), сохраняя данные в репозиторий.

